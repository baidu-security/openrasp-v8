addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-5
      - lcov
jdk:
  - openjdk7
go:
  - master
script:
  - mkdir build-base && pushd build-base
  - cmake -DCMAKE_BUILD_TYPE=Debug -DBUILD_COVERAGE=ON -DCMAKE_CXX_COMPILER=g++-5 ../base
  - make && make test && make coverage
  - ldd tests